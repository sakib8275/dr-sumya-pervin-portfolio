# Handoff — Dr. Sumya Pervin Portfolio

> **Read the Security History section before deploying anything.** A prior version of
> this document stated the site was "currently not deployed." That was wrong, and the
> error had consequences — see below.

## Project Overview

Single-page portfolio for Dr. Sumya Pervin, MD — a dermatologist and aesthetic medicine specialist based in Dhaka, Bangladesh. Built with vanilla HTML5, CSS3, and ES6+ JavaScript (no frameworks, no build tools). Backend is Cloudflare Pages Functions + D1 (SQLite) + R2 (images). Targets patients on mid-range Android phones (360–390px viewport) who want to book consultations.

---

## Architecture

### Hosting: Cloudflare Pages — and only Cloudflare Pages
- Static assets are served from `public/` **only**. Everything else in the repo (docs, migrations, `wrangler.toml`, `functions/` source) stays unpublished.
- API lives in `functions/` (Pages Functions)
- Custom domain: `drsumyapervin.com` (Cloudflare-managed)
- **Netlify is not a supported target.** It cannot execute Pages Functions, so every `/api/*` route fails there and booking, gallery, and the CMS all break. `netlify.toml` exists solely to make an accidental redeploy fail safely.

### Database: Cloudflare D1 (serverless SQLite)
- 4 tables: `admin_settings`, `appointments`, `gallery`, `contact_messages`
- Migration file: `migrations/001_schema.sql`
- Run via: `wrangler d1 execute dr-sumya-pervin-db --remote --file=migrations/001_schema.sql`

### Image Storage: Cloudflare R2
- Bucket: `dr-sumya-gallery`
- Gallery images uploaded via CMS and served at `/api/uploads/:filename`
- Uploads are restricted to JPEG/PNG/WebP at 2 MB, and served with `nosniff`

### Auth: JWT (stateless)
- Library: `jose` (HS256)
- Admin PIN is PBKDF2-SHA256, 100k iterations, per-install salt in `admin_settings.pin_salt`
- **The PIN plaintext is deliberately not recorded in this repository.** It is held by the practice. If it is lost, reseed `admin_settings` with a new hash and salt.
- Token stored in `localStorage` as `cms_token`, 24h expiry
- `JWT_SECRET` has no fallback. If it is unset, login fails with a 500 — by design.

### Frontend
- `public/index.html` — single-page app
- `public/css/style.css` — CSS custom properties design system, glassmorphism, full-width layout
- `public/js/main.js` — `api()` helper with auto JWT header, CMS panel, gallery render, booking form, quiz, slider, accordion, carousel
- `public/404.html` — real 404, so unknown paths don't silently return the homepage

---

## API Endpoints

| Endpoint | Method | Auth | Description |
|---|---|---|---|
| `/api/auth/login` | POST | No | Login with PIN, returns JWT |
| `/api/auth/check` | GET | No | Check JWT validity |
| `/api/appointments` | POST | No | Create booking (validated: `YYYY-MM-DD`, length caps) |
| `/api/appointments` | GET | JWT | List all bookings |
| `/api/appointments/:id` | PUT | JWT | Update booking status |
| `/api/appointments/:id` | DELETE | JWT | Delete booking |
| `/api/gallery` | GET | No | List gallery items |
| `/api/gallery` | POST | JWT | Add item (JSON or multipart + R2 upload) |
| `/api/gallery/:id` | DELETE | JWT | Remove from D1 + R2 |
| `/api/uploads/:filename` | GET | No | Serve image from R2 |
| `/api/config` | GET | JWT | Get CMS settings |
| `/api/config` | PUT | JWT | Update settings & PIN |
| `/api/config/public` | GET | No | WhatsApp + Telegram handles only — the booking form needs these |
| `/api/contact` | POST | No | Submit contact form |
| `/api/contact?from=SITE_SECRET` | GET | Secret | List messages |

---

## Security History

### 2026-07-28 — Public credential exposure (contained)

**What happened.** The repo was deployed to `drsumyapervin.netlify.app` with the publish directory defaulting to the repo root, and `wrangler.toml` set `pages_build_output_dir = "."`. Every file in the repo was therefore served publicly. A second copy was being served on the LAN by a stray `python3 -m http.server 8085`.

**What was exposed**, verified live with matching content-types and content-lengths:

| Path | Contents |
|---|---|
| `/migrations/001_schema.sql` | Admin PIN hash (unsalted SHA-256) |
| `/wrangler.toml` | `SITE_SECRET` in plaintext |
| `/agent.md`, `/HANDOFF-2026-07-28.md` | The admin PIN in plaintext prose |
| `/functions/**` | Full server source |
| `/AUDIT.md`, `/UX-AUDIT.md`, `/context.md` | Internal audit trail |

**Response taken.**
- Stray LAN server killed.
- Old PIN `talhatheboss` and old `SITE_SECRET` revoked; both must be treated as permanently compromised — they remain in git history.
- PIN hashing moved from unsalted SHA-256 to PBKDF2-SHA256 with a per-install salt.
- Site moved to `public/`; `pages_build_output_dir` scoped to it. Verified: sensitive paths now 404 with no secret strings in any response body.
- `netlify.toml` added to make an accidental Netlify redeploy fail closed.

**Containment closed 2026-07-29.** The Netlify site was deleted by the owner. Verified: `/`, `/wrangler.toml`, `/migrations/001_schema.sql`, `/agent.md` and `/functions/**` all return 404 with Netlify's platform-level `Not Found` body — not a site-level 404 page — and no response body contains any secret marker. Re-run the check with the loop under Deploy Steps, pointed at the Netlify host.

Both exposed credentials were rotated regardless and remain compromised in git history; treat any reference to the old PIN or `SITE_SECRET` as historical. Search-engine caches may still hold snapshots of the exposed content for some time.

---

## Completed Work

### Audit Round 1 — Security & Code Health (pre-migration)
Produced `AUDIT.md` and `FIXPLAN.md`; 8 fix commits. **Superseded** — audited the Express backend that no longer exists.

### Audit Round 2 — UI/UX (pre-migration)
Produced `UX-AUDIT.md` and `UX-FIXPLAN.md`, 24 findings, 13 P0/P1/P2 fixes (`8afdcf9`). **Partly superseded** — the "lying success states" finding regressed during the migration and was re-fixed in Round 3.

### Cloudflare Migration
Replaced the Express/SQLite/multer backend with Pages Functions + D1 + R2; deleted `server/` (35 files, ~1900 lines); created 14 function files, `migrations/001_schema.sql`, `wrangler.toml`. Committed as `7851663`.

### Audit Round 3 — post-migration (this pass)
See `AUDIT-ROUND-3.md`. Covers the exposure above plus the auth fallbacks, an unauthenticated stored XSS, and a booking flow that reported success on failure.

---

## Current State

- **Live at:** nowhere. Cloudflare — not yet deployed. Netlify — deleted 2026-07-29, verified gone.
- **GitHub:** `https://github.com/sakib8275/dr-sumya-pervin-portfolio` (private)
- **Workspace:** site in `public/`, API in `functions/`, everything else unpublished

---

## Deploy Steps (Cloudflare)

```bash
npx wrangler login

# Create D1 db & copy database_id into wrangler.toml
npx wrangler d1 create dr-sumya-pervin-db

# Create R2 bucket
npx wrangler r2 bucket create dr-sumya-gallery

# Run migration
npx wrangler d1 execute dr-sumya-pervin-db --remote --file=migrations/001_schema.sql

# Set secrets — note `pages secret`, not `secret`. Plain `wrangler secret put` is
# the Workers command and silently does nothing for a Pages project.
npx wrangler pages secret put JWT_SECRET
npx wrangler pages secret put SITE_SECRET

# Deploy
npx wrangler pages deploy
```

Then in Cloudflare Dashboard → Pages → connect `drsumyapervin.com`, and add a WAF rate-limiting rule on `POST /api/auth/login` (there is no application-level login throttle).

After deploying, confirm the exposure stays closed:

```bash
for u in /migrations/001_schema.sql /wrangler.toml /agent.md /functions/lib/auth.js; do
  curl -s -o /dev/null -w "$u %{http_code}\n" "https://drsumyapervin.com$u"; done   # all 404
```

---

## Unresolved / Blocked (Owner Action Required)

| Item | What's Needed |
|---|---|
| **Admin PIN** | **Lost.** It was never recorded in any file and the holder does not have it. No remote D1 exists yet, so regenerate the salt + PBKDF2 hash in `migrations/001_schema.sql` *before* the first migration run — the seed is `INSERT OR IGNORE`, so afterwards it needs an `UPDATE` against live D1 instead. |
| **Real WhatsApp number** | The nav and floating button now populate from the CMS and stay hidden until a number is set. Nothing is displayed until the doctor enters one. |
| Before/after slider | Real clinical before-and-after photos (not generic images) |
| Testimonial faces | Real patient photos (with consent) — currently reusing 3 generic images |
| Certificate images | Scans of actual degree certificates (10 slots, all use same 3 images) |
| Gallery default items | Placeholder items reusing the same 3 images — replace with real clinical photos |
| Stats "1500+ procedures" | This is a medical advertising claim on a physician's site, not just a placeholder. Source it or remove it. |

---

## Known Issues / Gotchas

- **`JWT_SECRET` must be set before deployment.** Login returns 500 without it. There is deliberately no fallback — the previous fallback constant meant anyone reading the source could mint admin tokens.
- **`SITE_SECRET` must be set** or `GET /api/contact` returns 500 rather than falling back to a default.
- **D1 migration must run before the site works.** There is no runtime seeder any more; one that invents a default PIN is a backdoor.
- **No signup flow** — one admin PIN, changeable via CMS Settings (requires current PIN, minimum 8 characters). If lost, reseed via a D1 query.
- **No login rate limit in code.** Add the Cloudflare WAF rule.
- **Booking flow:** bookings persist to D1, and the doctor must check the CMS — no push notification exists. If the API call fails the patient now sees an explicit failure, not a fabricated reference number.
- **Hero gradient contrast** is inherently fragile — any change to hero gradient colors needs contrast re-verification.
- **Anything added to the repo root is unpublished by default.** New patient-facing files go in `public/`.

---

## Codebase Navigation

| File | Role |
|---|---|
| `public/index.html` | SPA — hero, stats, chambers, quiz, services, gallery, testimonials, FAQ, footer, modals, CMS admin |
| `public/404.html` | Not-found page |
| `public/css/style.css` | Design system via CSS custom properties |
| `public/js/main.js` | All interactive logic + `api()` helper with JWT |
| `functions/_middleware.js` | CORS (origin-scoped via `ALLOWED_ORIGIN`) |
| `functions/lib/auth.js` | JWT sign/verify, PBKDF2 PIN hashing, constant-time compare, JSON helpers |
| `functions/api/auth/login.js` | POST — login |
| `functions/api/auth/check.js` | GET — JWT check |
| `functions/api/appointments/index.js` | GET (auth) list, POST create + validation |
| `functions/api/appointments/[id].js` | PUT status, DELETE |
| `functions/api/gallery/index.js` | GET list, POST (auth) add + MIME/category validation |
| `functions/api/gallery/[id].js` | DELETE (auth) |
| `functions/api/uploads/[filename].js` | GET serve from R2 with `nosniff` |
| `functions/api/config/index.js` | GET/PUT settings + PIN change |
| `functions/api/config/public.js` | GET — WhatsApp/Telegram only, unauthenticated |
| `functions/api/contact.js` | POST submit, GET list (secret) |
| `migrations/001_schema.sql` | D1 schema + admin seed |
| `wrangler.toml` | Cloudflare bindings; **no secrets** |
| `netlify.toml` | Fail-closed guard against accidental Netlify redeploy |
| `context.md` | Domain background & practitioner profile |
| `AUDIT.md`, `FIXPLAN.md` | Round 1 (superseded) |
| `UX-AUDIT.md`, `UX-FIXPLAN.md` | Round 2 (partly superseded) |
| `AUDIT-ROUND-3.md` | Round 3 — post-migration security pass |
| `agent.md` | Detailed agent onboarding doc |

---

## Suggested Future Work (Out of Scope, Not Started)

- **Integration tests.** There are none. `package.json` has one dependency and no scripts. The migration shipped three regressions that a smoke test over `/api/config` and `/api/appointments` would have caught.
- **Structured data (JSON-LD):** `MedicalBusiness` and `Physician` schema — none exists.
- **Open Graph / social preview:** no `og:` tags, so link previews fail on WhatsApp/Facebook.
- **Push notifications:** nothing tells the doctor a booking arrived.
- **Booking confirmation page** instead of a modal flash.
- **Bengali language toggle** — the site is English-only.
- **Analytics** — a privacy-preserving option (Plausible, Fathom).
- **Service worker / offline** for returning patients on slow connections.
