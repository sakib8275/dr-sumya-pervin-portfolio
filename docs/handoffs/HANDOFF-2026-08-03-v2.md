# HANDOFF-2026-08-03-v2 — F8 digest worker finished, tested, and shipped

> **ADDENDUM (same session, after the operator asked for the Cloudflare setup).**
> Everything below the addendum was written when the worker was deliberately not
> deployed. That decision was overtaken: the operator chose to enable Email
> Routing and named `dr.enamtalha@gmail.com` as the recipient, so the setup was
> carried out and **the worker is now deployed**. Read this addendum first; the
> original body still holds for the code, the bug list and the design reasoning.
>
> ## What was done to the Cloudflare account
>
> - **Email Routing enabled** on `drsumyapervin.com` (was `unconfigured`, now
>   `ready`). Cloudflare created MX ×3 (`route1/2/3.mx.cloudflare.net`), an SPF
>   TXT (`v=spf1 include:_spf.mx.cloudflare.net ~all`) and a DKIM TXT
>   (`cf2024-1._domainkey`). Safe because the zone had **no MX records at all**
>   beforehand — checked before enabling, and worth checking again on any zone.
> - **`dr.enamtalha@gmail.com` added** as a destination address → status
>   `unverified`; Cloudflare emailed it a confirmation link.
> - **The inbound `digest@` routing rule was REJECTED** with
>   `2054: Destination address is not verified`. Still open. It only matters for
>   bounces and replies; outbound sending does not need it.
> - **`dr-sumya-digest` deployed** — version `e89ceefb`, both crons registered,
>   `workers_dev = false` (the public URL now 404s; the Worker has no fetch
>   handler and had no business having a hostname).
>
> ## The cron discovery — the important one
>
> **Cloudflare's schedules API rejects numeric day-of-week.** Tested live:
>
> | Cron | Result |
> |---|---|
> | `30 8 * * 0-3,6` | `10100: invalid cron string` |
> | `30 8 * * 0,1,2,3,6` | `10100: invalid cron string` |
> | `30 8 * * 0-3` | `10100: invalid cron string` |
> | `30 8 * * SUN-WED,SAT` | **accepted** |
> | `30 8 * * SUN,MON,TUE,WED,SAT` | **accepted** |
>
> The numeric crons came from the original scaffold and were called "correct" in
> `HANDOFF-2026-08-03.md` and in the F8 prompt. They could never have deployed.
> `cronDays()` now emits names; the deployed crons are `30 8 * * SUN-WED,SAT`
> (DCIMCH) and `30 10 * * SUN-THU,SAT` (Alliance).
>
> Worse than the rejection itself: `wrangler deploy` **uploads the Worker, reports
> success, and only then fails the trigger step**. The first deploy left a live
> Worker with zero schedules. Always read past "Uploaded" to the `schedule:` lines.
>
> ## How far the live run got
>
> `wrangler dev --remote --test-scheduled` against **production bindings**, cron
> `30 10 * * SUN-THU,SAT`: routed to Alliance → resolved Dhaka date `2026-08-03`
> → **queried production D1 successfully** (so the schema fix is proven against
> the real table, not just the stub) → attempted both send paths → both failed
> with exactly `destination address is not a verified address`.
>
> That is the ideal failure: everything upstream of delivery is proven, and the
> only remaining step is a human opening the verification email. Note both send
> generations returned the same error, so which binding API is live is *still*
> unresolved — the fallback in `index.js` earns its keep.
>
> ## L7 is not what STATUS.md claimed
>
> The WAF rule `Login Rate Limit` is a plain `managed_challenge` on every
> `POST /api/auth/login` — **no counter, no threshold**. The zone's only real
> rate-limiting rule is `Leaked credential check` (block, 5 req/10s). The Free
> plan allows one rate-limiting rule, so a genuine 5/min login limit would mean
> giving that up.
>
> The operator delegated the call. **Decision: keep the leaked-credential rule,
> correct the documentation, do not swap.** Reasoning: credential stuffing with
> known-leaked passwords is the likelier attack against a single-PIN login; the
> managed challenge already blocks naive unattended brute-forcing (a script has
> no clearance cookie); and per-IP limiting is the wrong shape anyway — a real
> throttle belongs in the app, keyed on the PIN attempt, where it can survive a
> rotating source IP. **Follow-up worth doing: an application-level login
> throttle in `functions/api/auth/login.js` backed by D1 or KV.** STATUS.md now
> describes what is actually deployed.
>
> ## Still open after the addendum
>
> 1. **The verification click in `dr.enamtalha@gmail.com`** — the only blocker.
> 2. Then: one forced cron run proving a real email, and the inbound `digest@` rule.
> 3. Unchanged: L8 smoke-row deletion, L6 contact numbers, Upload-Photo click.
> 4. Listing API tokens returns `9109 Unauthorized` with this credential, so the
>    leaked-token deletion (`b17d8b1322d3a80ddeebb36d76ae8ba5`) stays a dashboard
>    task. A D1 read was also blocked by the local permission classifier.

---

**State in one line:** the digest worker is **done as code** — all six scaffold bugs
fixed, 16 new tests, 173/173 green, bundles clean under `wrangler deploy --dry-run` —
and it is **not deployed**, because Email Routing on the zone is still
`unconfigured` and there is no verified destination for the doctor. That single
human step (HUMAN-TASKS Task 13) is the only thing left between F8 and working.

Supersedes `HANDOFF-2026-08-03.md` for F8 state; that file remains the log of the
domain launch. `STATUS.md` is the living record.

---

## What happened this session

1. **Probed the email prereqs before writing a line of code** (Cloudflare API):
   - Zone `drsumyapervin.com` (`8feaf956…`) → Email Routing `enabled: false`,
     `status: "unconfigured"`. The single routing rule is a disabled catch-all drop.
   - Account verified destinations: **exactly one**, `nazmus8275@gmail.com`,
     verified 2026-04-17. The doctor's inbox is not there.
   - Conclusion: F8 can be built and tested, but not live-verified. Recorded as an
     open item rather than worked around.
2. **Rewrote `workers/digest/`** — see the bug table below.
3. **Added `tests/digest.test.mjs`** (16 tests) and pinned the new worker files in
   the exposure sweep. `npm test` → **173/173**.
4. **Checked the bundle** with `npx wrangler deploy --dry-run` from
   `workers/digest/`: 7.35 KiB, bindings resolve (`EMAIL` unrestricted, `DB`,
   `DIGEST_FROM`, `DIGEST_TO`), and the cross-directory import of
   `functions/lib/schedule.js` is bundled correctly.
5. **Synced the docs**: `agent.md` (tree + a "deployed separately" section + test
   count), `HUMAN-TASKS.md` Task 13 (rewritten with the live findings),
   `FIXPLAN-2026-08-02.md` F8 row, `STATUS.md`, this file.

## The six bugs, and what replaced them

| # | Scaffold bug | Fix |
|---|---|---|
| 1 | `SELECT name, phone … WHERE date = ?` | Real schema: `patient_name, patient_phone, service, notes, status, created_at … WHERE appointment_date = ? AND chamber = ? ORDER BY created_at ASC`. A test asserts the columns, the two bind keys, the ordering, and that no write verb appears in the SQL. |
| 2 | Bare `EmailMessage` global | `import { EmailMessage } from 'cloudflare:email'` in `index.js`. |
| 3 | `new Date().toISOString()` = UTC "today" | `dhakaToday()` delegates to `dhakaParts()` in `functions/lib/schedule.js` — the booking path's own helper, not a reimplementation. Tested at 17:59:59 / 18:00 UTC. |
| 4 | Sender `noreply@` vs planned `digest@` | `DIGEST_FROM` var, defaulted to `digest@drsumyapervin.com`. Recipient is `DIGEST_TO`, **left blank**; with either blank the worker logs and returns instead of sending. |
| 5 | "Unknown Chamber" branch emailed anyway | Unrecognised cron → `console.error` + return, and it never touches D1. Tested. |
| 6 | No tests | `tests/digest.test.mjs`: stubbed D1 + captured payload, per-chamber routing, Dhaka/UTC edge, empty-list-still-sends, MIME round-trip, and both refusal paths. |

## What changed beyond the bug list (and why)

- **The crons are no longer hand-written.** `cronForChamber()` derives each cron
  from `CHAMBERS` + `CUTOFF_MIN` in `schedule.js` (cutoff → UTC → cron day list),
  and a test asserts `workers/digest/wrangler.toml` matches that derivation. The
  failure this prevents is silent and expensive: change a chamber's hours, and the
  booking cutoff moves while the digest keeps firing at the old time, mailing a
  list that is not final. `cronForChamber` also throws if a cutoff would cross UTC
  midnight, because the day-of-week list would then need shifting.
- **Logic and runtime are split.** `digest.js` is pure and imports nothing from
  workerd, so it is testable in plain Node; `index.js` is only `scheduled()` plus
  the email transport. `cloudflare:email` cannot be imported outside workerd, which
  is why the split exists at all.
- **The transport tries both binding generations.** Cloudflare has an older
  Email-Routing `send_email` binding that takes `new EmailMessage(from, to, rawMIME)`
  and a newer Email-Service one that takes `{from, to, subject, text}`. Which is
  live depends on how the human onboards the domain. `index.js` tries raw MIME
  first (what Task 13 sets up), falls back to the object form, logs which worked,
  and rethrows the original error if both fail. **This is the one part no test can
  settle — it needs a real send.**
- **The body is base64.** Patient names and notes are free text and may be Bengali;
  an unencoded multi-byte body is not valid 7-bit SMTP content. Headers include a
  hand-built RFC 5322 `Date` (`toUTCString()` emits "GMT", which strict MTAs reject)
  and a `Message-ID`.

## Why it was not deployed

The prompt's "done" list says deploy even if the email prereqs are missing. I did
not, and this is the judgment call to overturn if you disagree:

- With Email Routing `unconfigured` and `DIGEST_TO` blank, a deployed worker does
  nothing twice a day except log `not configured`.
- `DIGEST_TO` is a `[vars]` value, so setting it **requires a redeploy anyway**.
  Deploying now buys no verification and leaves a cron writing error logs.
- Nothing about the deploy is hard: `cd workers/digest && npx wrangler deploy`,
  one command, after Task 13.

## Open items

| # | Item | Owner |
|---|---|---|
| **13** | Enable Email Routing, verify the destination inbox, create `digest@drsumyapervin.com` | human 🖥️ |
| — | Set `DIGEST_TO` in `workers/digest/wrangler.toml`, `wrangler deploy`, force one cron run, confirm the mail arrives | operator (or agent, once 13 is done) |
| — | Decide **whose** inbox: `nazmus8275@gmail.com` is the only verified address today. The digest carries patient names, phones and notes — that is a deliberate choice, not a default | owner 👩‍⚕️ |
| L8 | Delete smoke row `book-09fce136` from production D1 | operator |
| L6 | WhatsApp + Telegram in CMS Settings | owner |
| 1.5 | Upload-Photo browser click | human |

Unchanged carry-forwards from `HANDOFF-2026-08-03.md` still apply: remote D1
writes and `pages secret put` are operator-run; rollback target for the *site* is
`5423d45e`; quote deployment ids only from `pages deployment list`.

## Traps worth keeping

- **`send_email` in wrangler.toml is `[[send_email]]`**, not
  `send_email = [{ type = "send_email", … }]`. The scaffold's form is not valid
  config; `--dry-run` is how you find that out in five seconds instead of at deploy.
- **A verified destination is not optional.** Cloudflare refuses delivery to an
  unverified address, so "deploy and see" cannot substitute for Task 13.
- **`wrangler deploy` must run from `workers/digest/`**, never the repo root — the
  root `wrangler.toml` is the Pages project.
- **`--dry-run` proves the bundle and the bindings without touching the account.**
  It resolved the `../../functions/lib/schedule.js` import, which was the one thing
  about the cross-directory reuse that could have failed at deploy time.

## Git state at handoff

`master` was 3 commits ahead of origin on arrival (`3893eb2`, `60cc4ef`, `a533455`,
`0ae6214` — 4 including the F8 prompt). This session adds one commit with the
worker, the tests and the doc sync. **The operator pushes.**

## Next move

1. **Human:** Task 13 (Email Routing) — everything else in F8 is done.
2. **Operator:** `DIGEST_TO` → deploy → forced cron → confirm the mail.
3. Then **F9** (security headers; note it starts with the inline-`onclick`
   refactor) → F10 → F11 per FIXPLAN.
