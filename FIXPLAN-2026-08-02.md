# FIXPLAN-2026-08-02.md — Launch & Hardening Plan

**Repo:** `/home/kitahara-poposagain/Desktop/Portfolio Sumya Pervin` · **Branch:** `master`
**Basis:** read-only launch-readiness audit run 2026-08-02 (evidence appendix at the bottom; it predates Phase 1).
**Status:** **Phase 1 shipped** 2026-08-02 (deploy `f64b9221`). **Phase 2 shipped 2026-08-04** (deploys `fb1b3aa8` + `42aa5567`) — F8 code, F9, F10, F11, F12 all done; the only thing left in the whole plan is **observing F8's first scheduled run**. Live state: `STATUS.md`.

## Progress log

- **2026-08-04 — Phase 2 complete.** Two deploys. `fb1b3aa8` shipped the pending
  UX batch and was verified on the live apex with a **real Turnstile token**:
  confirmation-only success modal, all four quiz outcomes reaching a valid
  `#serviceType.selectedIndex` (the critical fix — every quiz booking used to be
  server-rejected), a 375px pass, the no-JS render, and zero CSP violations.
  `42aa5567` shipped F11's structured write logs and a bug F10 caught on its
  first run: `.fab-btn { display: grid }` outranked the `[hidden]` attribute, so
  the WhatsApp FAB's gating never actually hid it. **F10** = 14 Playwright tests
  on the Miniflare harness rather than `wrangler pages dev`, which cannot
  intercept siteverify. **F11** = CI gate (no auto-deploy), 30-minute uptime
  probe, a backup runbook whose restore was actually drilled, and JSON write logs
  proven in production carrying no patient data. Tests: **204 node + 14 e2e.**
  ⚠️ Three deploy-verification bookings are sitting in production D1 and only the
  operator can delete them — see `STATUS.md` owner action #0, and note the F8
  digest will email them to the doctor if they are still there on their dates.

- **2026-08-04 — L6 closed; digest instrumented.** No feature code changed. The
  owner supplied `+8801725196101`; it was written **straight to production D1**
  (the CMS route needs the operator-only PIN) and reads back on
  `/api/config/public`, so booking confirmations now carry a real `wa.me` link.
  **Telegram is deferred by the owner** — and the code reading found why that
  costs nothing today: `main.js:726` builds a generic `t.me/share/url` sheet that
  never routes to the doctor, so the stored `telegram` value drives nothing. A
  real integration needs a code change, not just a username. Separately, Workers
  Logs were enabled on `dr-sumya-digest` (version `0f6d80cd`) **before** its
  first scheduled run, because with 0 appointments in production D1 a healthy
  quiet run and a broken worker produce the identical observation: no email.
- **2026-08-03 — F9 complete.** Deploy `74c3bd4b`; 191/191 tests. Zero inline
  event handlers left in `public/`; CSP, HSTS, Permissions-Policy and the framing
  headers live on the apex. Two rounds were needed: the first deploy was correct
  for everything in this repo and still broke the two scripts **Cloudflare's zone
  injects into the apex HTML** — invisible on `pages.dev` and therefore invisible
  to every local and preview test. See the F9 row for the fix. Next: F10.
- **2026-08-02 — Phase 1 complete.** Commits `559b515`…`70a53ff` (pushed by operator).
  Deploy A `f64b9221` verified on pages.dev: 148/148 tests green (20 new: cutoff
  boundaries, weekday rules, Dhaka/UTC crossover, 409 duplicate guard, R2 orphan
  cleanup); tokenless booking 403; exposure paths 404; OG/favicon/robots/sitemap
  live; 1500+ stat gone. F4's schedule rules live in `functions/lib/schedule.js`
  (F8 will reuse them). **Outstanding from the gate:** one human browser click
  each on CMS Update Status and Upload Photo.

---

## Locked decisions (owner/operator, 2026-08-02)

1. **Scope:** everything, P0–P2.
2. **www:** 301 redirect `www.drsumyapervin.com` → apex (zone Redirect Rule; no second hostname, no dual-origin).
3. **T11 notification:** booking cutoff **30 min before consultation start**, and a **daily per-chamber email digest** to Dr. Sumya after that chamber's cutoff.
4. **11 MB PNG:** delete from working tree only — no git history rewrite.
5. **"1500+ procedures":** remove now, restore only when the practice sources it.

## Verified constants the plan builds on

- Chamber values the server receives (`public/index.html:484-485`):
  - `Alliance Hospital Limited (Shyamoli)` — Sat–Thu 17:00–20:00 Dhaka → cutoff **16:30 Dhaka = 10:30 UTC**
  - `Dhaka Central International Medical College (DCIMCH)` — Sat–Wed 15:00–17:00 Dhaka → cutoff **14:30 Dhaka = 08:30 UTC**
  - Dhaka = UTC+6, no DST; hardcoding the offset is safe (comment it).
- Serving deployment `5423d45e` (source `3b3dadc`); rollback target **`d3a4d921`**; HEAD `462a0f2` (www in `TURNSTILE_HOSTNAMES`, undeployed, harmless under the 301 decision).
- CSP blockers: 3 generated inline `onclick` (`public/js/main.js:1117,1170,1171`) + 8 static in `index.html`; 55 inline `style` attrs; no Escape-key modal handler exists; 17 `<label>`s present.
- Turnstile widget `0x4AAAAAAEClxf8-TRYoLcZl`; widget hostname edits are **dashboard-only** (OAuth API mutation rejected `10405`).

**Sequencing logic:** Phase 1 code ships and is verified on `pages.dev` *before* any dashboard step, so domain-launch smoke tests exercise the final build and code + domain are never debugged simultaneously.

---

## PHASE 1 — Code fixes, one batched deploy (agent, ~1 day)

| # | Fix | Spec | Verify | Risk |
|---|---|---|---|---|
| F1 | **Remove "1500+" claim** | Delete the `stat--c` card (`public/index.html:88-93`); 2-stat grid remains. Grep-verified the only occurrence. | `grep -c "1500" public/index.html` → 0; grid clean at 360px | Low |
| F2 | **Gallery R2/D1 orphan** | `functions/api/gallery/index.js`: try/catch around the D1 insert (`:88-90`); on failure `GALLERY_BUCKET.delete(filename)` (only when a file was stored), log, 500. | New harness test: DB stub throws on INSERT → bucket empty + 500 + no row | Low |
| F3 | **Import-path bug** | `public/js/main.js:1032-1050`: accept `image_path` only if it passes the API's rule (`/api/uploads/…` or http(s)); replace both silent `catch (e) {}` with per-item tally + summary alert ("N imported, M skipped"). | Backup JSON with an `assets/clinic.jpg` item → skipped + counted, not silently dropped | Low |
| F4 | **Booking cutoff + day/date/duplicate validation** | New `functions/lib/schedule.js`: `CHAMBERS` map (exact strings above) + pure `validateSlot(chamber, dateStr, nowDhaka)` → ok / past-date / closed-that-day (names next open day) / cutoff-passed. Wire into `functions/api/appointments/index.js` (400 with message). Duplicate guard: same phone+date+chamber → 409. `main.js`: `min` on date input; failure box shows the server's `err.message` when present (`:722-735`). | Pure-fn unit tests: 14:29 vs 14:30 Dhaka, Friday (both chambers closed), Thursday DCIMCH, UTC/Dhaka crossover. Integration: valid-tomorrow → 201; violations → 400/409 with **nothing written** | Medium — patient-facing wording; owner signs off schedules |
| F5 | **SEO/OG** | `<head>`: canonical `https://drsumyapervin.com/`, og:title/description/url/type/image (hero_portrait initially), twitter card, JSON-LD `Physician` (degrees, SSMC affiliation, both chambers as `location`); add `robots.txt`, `sitemap.xml`, SVG favicon. | Post-deploy: grep served head for all tags; browser validator pass | Low |
| F6 | **Housekeeping** | `git rm` `screencapture-*.png` + `screenshot.png`; commit. `netlify.toml` stays (fail-closed guard). History untouched per decision 4. | Root clean | Low |
| F7 | **Accessibility pass** | (a) privacy line above booking submit; (b) modal Escape-close + focus trap (agent.md requires it; none exists); (c) verify labels cover every control; (d) hero contrast spot-check at the orange end; (e) focus-visible sweep. Use `frontend-a11y` skill. | Keyboard-only booking walkthrough; axe DevTools → 0 critical | Low |

**Gate before deploy A:** `npm test` green (incl. new F2/F4 tests) → `node --check main.js` → `npx wrangler pages deploy` → on `pages.dev`: 200/~36 kB; tokenless booking still 403; one widget booking 201; exposure paths still 404 (1,512 B); OG tags live; **click the two never-browser-verified CMS flows** (Update Status cycle; Upload Photo → published photo is the chosen file).

---

## PHASE 0 — Go-live on drsumyapervin.com (human dashboard, ~1 h + smoke)

| # | Step | Verify-after | Who |
|---|---|---|---|
| L1 | ✅ ~~Pages → Custom domains → `drsumyapervin.com`; wait cert **Active**~~ | `curl https://drsumyapervin.com/` → 200, ~36 kB, grep "Dr. Sumya Pervin" | **Human** |
| L2 | ✅ ~~Turnstile widget → hostnames += `drsumyapervin.com` (apex only; www redirects)~~ | Real anonymous browser booking → **201 + `book-xxxxxxxx`** | **Human** (API blocked) |
| L3 | If booking 403s → **rollback to `5423d45e` first, debug after** | pages.dev booking path healthy | Human |
| L4 | ✅ ~~Redirect Rule: `www.drsumyapervin.com/*` → 301 `https://drsumyapervin.com/$1`~~ | `curl -I https://www…` → 301 | Human |
| L5 | ✅ ~~SSL/TLS → Full (strict) + Always Use HTTPS~~ | `curl -I http://…` → 301 https | Human |
| L6 | Owner: CMS Settings → real WhatsApp digits + Telegram `@username` (numeric TG IDs don't resolve) | `/api/config/public` returns both; FAB visible; booking confirmation shows working `wa.me` button | **Owner** |
| L7 | ✅ ~~WAF rate-limit `POST /api/auth/login`, 5/min/IP, **Managed Challenge**~~ | 6 rapid bad POSTs → 6th challenged; one real login succeeds | Human |
| L8 | Operator deletes the smoke booking row from production D1 | `appointments` count back to expected | Operator |


---

## PHASE 2 — Hardening & ops (~1.5 agent days + ~30 min human email setup)

| # | Item | Spec | Verify | Who |
|---|---|---|---|---|
| F8 ✅ *code* ✅ *deployed* ✅ *unblocked* / ⬜ *first live run observed* | **Daily digest email (T11)** — **2026-08-03: built, 16 tests green, DEPLOYED** (`dr-sumya-digest`, live version **`0f6d80cd`**, both crons registered, no public URL, **Workers Logs on**). Email Routing enabled on the zone, `dr.enamtalha@gmail.com` set as `DIGEST_TO` and **`verified` 2026-08-03 16:13 UTC** — the send path is open and nothing blocks the worker. Before that click, a forced run reached the send and failed only with "destination address is not a verified address". **Remaining: observe the first scheduled run** (2026-08-04 08:30 UTC / 14:30 Dhaka; no cron had fired as of the 15:10 UTC deploy) — read the Workers Logs, not the inbox, because production D1 has 0 appointments and a correct run will therefore send no mail at all. Cron day-of-week had to become names (`SUN-WED,SAT`); the numeric form never deployed. All six scaffold bugs fixed (schema columns, `cloudflare:email` import, Dhaka date, `digest@` sender, unknown-cron no longer emails, tests). Crons are now *derived* from `functions/lib/schedule.js` and asserted against `wrangler.toml`, so a schedule change cannot leave the digest on the old cutoff. Deploy deliberately deferred: Email Routing on the zone is **`unconfigured`** and the only verified destination on the account is the operator's `nazmus8275@gmail.com`, so a deployed worker would only log "not configured" twice a day and need redeploying anyway once `DIGEST_TO` is set. **Open: the verification click in `dr.enamtalha@gmail.com`, then one forced cron run to prove a real email, and the inbound `digest@` rule.** | New `workers/digest/` (own wrangler.toml; D1 binding read-only; `send_email` binding). Cron `"30 8 * * 0-3,6"` (DCIMCH) + `"30 10 * * 0-4,6"` (Alliance). Handler: today's rows for the chamber whose cutoff fired → email (name, phone, service, notes, status, time-ordered); **always send, even "0 bookings"** (silence ≈ failure). Never in the booking path. ⚠️ Verify Email Sending binding availability against current Cloudflare docs at build time; fallback = third-party API secret. Human prereqs: enable Email Routing on the zone, verify destination, create `digest@drsumyapervin.com`. | Unit tests: stubbed D1 + captured payload, per-chamber filter, Dhaka/UTC edge, empty-list body. Live: forced cron run → email arrives | Agent + **human** (email setup) |
| F9 ✅ **DONE 2026-08-03** | **Security headers** | **Shipped** as deploy `74c3bd4b`. The inline-handler count in this row was wrong: **12, not 11** — 8 static `onclick` + **1 static `onsubmit`** (the booking form's load-bearing `onsubmit="return false"`, which this plan never counted) + 3 generated `onclick`. All replaced with delegated listeners and `data-cms-action`/`data-cms-id`; the `onsubmit` guard moved to a new blocking `public/js/formguard.js` in `<head>`. Removing `onclick="cmsLogout()"` also **fixed a button that had never worked** — `cmsLogout` is declared inside main.js's DOMContentLoaded callback, so the inline handler always threw ReferenceError. CSP shipped as specified plus `object-src 'none'`, `base-uri 'self'`, `form-action 'self'`; `default-src`/`connect-src`/`font-src` deliberately left unset (Turnstile makes its own requests, incl. to `*.challenges.cloudflare.com`, and a wrong `connect-src` breaks bookings silently). HSTS sent only after L5 was re-verified live; `includeSubDomains` justified against the zone's DNS (apex + www only); **no `preload`**. **The one thing local testing could not catch:** the zone injects two scripts into the *apex* HTML that are not in this repo — Cloudflare JavaScript Detections (inline, embeds a per-request ray id, so no hash can ever match, and Bot Fight Mode makes it non-disableable) and the Web Analytics beacon. The first deploy blocked both. Fixed per Cloudflare's documented route: a **per-request CSP nonce**, which their CDN parses from the response header and stamps onto its injected script (verified: header nonce == injected tag nonce), plus `static.cloudflareinsights.com`. 18 new tests, incl. a source scan that fails if any `on*=` attribute returns to `public/`. | ✅ `npm test` 191/191 · ✅ `curl -I` on apex · ✅ exposure sweep re-run, all 404, no secret markers · ✅ tokenless booking still 403 · ✅ **0 CSP violations** in a full local click-through *and* a full production pass · ✅ real booking completed locally end to end (Turnstile → siteverify → D1) · ⚠️ production CMS not logged into (operator-only credential and writes) | Agent |
| F10 ✅ **DONE 2026-08-04** | **Playwright DOM layer** | **Shipped** as `tests/e2e/` + `playwright.config.mjs`, **14 tests** across the five required areas, `npm run test:e2e`. **The runner changed deliberately: not `wrangler pages dev`.** That runner cannot intercept the outbound siteverify call, and `verifyTurnstile` demands both the right action and an allowlisted hostname — so every DOM booking test would have 403'd for a reason with nothing to do with the DOM. The tests instead start the **same Miniflare harness the API suite uses** (`tests/helpers/harness.mjs`), which already solves exactly this and also listens on a real HTTP port: one siteverify stub, one migration path, one route table, real Chromium against the real compiled Worker. Turnstile is stubbed page-side and derives its token **from the widget's action** — a single fixed token passes booking and silently 403s login. Also absorbed the UX batch's assertions per the audit. Kept out of `npm test` so the deploy gate stays ~3 s. **F10 found a real latent bug on its first run**: `.fab-btn { display: grid }` outranked the `[hidden]` UA rule, so `main.js:164`'s `fab.hidden = !digits` hid nothing and an unconfigured WhatsApp number would have rendered a live `href="#"` button. Fixed with `[hidden] { display: none !important; }`. | ✅ 14/14 green · ✅ bug caught and fixed · ✅ `tests/exposure.test.mjs` extended to cover the new files | Agent |
| F11 ✅ **DONE 2026-08-04** | **Ops** | **Shipped.** `.github/workflows/ci.yml` — `npm test` + `npm run test:e2e` on push/PR; **deploys stay manual on purpose**, because the two failure modes that have actually bitten this project (a Turnstile hostname list that rejects every booking, a CSP that blocks the widget) are invisible to everything that runs before a deploy. `.github/workflows/uptime.yml` — 30-min probe asserting the **JSON shape** of `/api/config/public` (a catch-all can return the homepage with a 200), the homepage, and that HSTS + CSP survive. `npm run backup:d1` + `docs/RUNBOOK-BACKUP.md` — the script's checks are the value, not the export: it fails on a missing table but treats **0 rows as correct**, since production D1 is often empty and conflating the two would train the operator to ignore its warnings. `functions/lib/log.js` — one JSON line per D1 write, with an explicit never-log list (patient name/phone/notes, PINs, hashes, secrets, tokens) and errors logged by `message` only, because a D1 stack can carry bound values that are patient data. | ✅ CI red proven locally (rename `validateSlot` → exit 1; revert → exit 0) · ✅ backup exported 4/4 tables and **restored into a wiped local D1 with matching counts** · ✅ all three uptime probes dry-run green against production · ✅ a live apex booking emitted the JSON line, **carrying no patient name or phone**, read back via `wrangler pages deployment tail` · ⚠️ the workflows have **not** run on GitHub yet (unpushed) | Agent |
| F12 ✅ **DONE 2026-08-04** | **Docs consolidation** | Structure was already in place (`docs/handoffs/`, `docs/prompts/`, one living `STATUS.md`). This pass did the reconciliation: **`agent.md`** gained §5b (the F10 DOM layer, why not `pages dev`, the `:visible` trap) and §5c (F11's four surfaces plus how to actually read Pages Functions logs — `pages deployment tail` with a **full uuid**; `--environment production` alone is rejected non-interactively). **`STATUS.md`** re-pointed at both new deploys, the real test counts, the three smoke rows and the still-unobserved digest run. `docs/handoffs/HANDOFF-2026-08-04-v3.md` added and `docs/README.md` re-pointed. | ✅ next agent reads one file | Agent |

---

## Owner questions that gate specific items

1. ~~**Digest email address** for Dr. Sumya~~ — **ANSWERED**: `dr.enamtalha@gmail.com`, added as an Email Routing destination and `verified` 2026-08-03 16:13 UTC. No longer blocks F8.
2. **Confirm chamber schedules exactly as printed** (Alliance Sat–Thu 5–8 PM; DCIMCH Sat–Wed 3–5 PM) — F4 is only as correct as these. **Still open.**
3. ~~**WhatsApp number**~~ — **ANSWERED 2026-08-03**: `+8801725196101`, live in production D1; **L6 closed**. **Telegram is deferred by the owner** and is not a blocker: the button is a generic share sheet that never routes to the doctor, so a username alone would change nothing without a code change at `main.js:726`.
4. Standing: delete leaked token id **`b17d8b1322d3a80ddeebb36d76ae8ba5`** — match by **id in the token page URL**, not name; then delete the commented `~/.bashrc` line.
5. One-glance R2 dashboard check: any object in `dr-sumya-gallery` without a D1 row is an orphan (D1 shows 0 gallery rows; CLI cannot list objects in wrangler v4).

**Estimates:** Phase 1 ≈ 1 agent-day + deploy A · Phase 0 ≈ 1 h human + 15 min smoke · Phase 2 ≈ 1.5 agent-days + 30 min human + deploy B.

---

## Appendix — audit evidence (2026-08-02, all re-verified, none assumed)

| Area | State | Evidence |
|---|---|---|
| Tests | **128/128 PASS** | `npm test`, 2.86 s, Miniflare + real compiled worker |
| Route contract | Clean | All 13 `api()` call sites map to real Functions; both 2026-08-02 fixes live (only comments mention the dead routes — `main.js:924,1185`) |
| Production | `5423d45e` serving | Handoff named `b4d74a16` — stale record, docs-only delta, no code drift. **HEAD `462a0f2` undeployed.** |
| Turnstile live | Fail-closed verified | Tokenless booking POST → 403 `{"error":"Verification failed…"}`; code checks success+action+hostname (`functions/lib/turnstile.js:51-57`) |
| Auth surface live | Verified | Anon: `/api/appointments` GET → 401, `/api/config` → 401, `/api/config/public` → 200 |
| CORS | Correct | Foreign Origin GET → no ACAO; apex echoed. (`ACAO:*` was HEAD/static-asset platform noise, not the middleware) |
| Exposure | Closed | `/wrangler.toml`, `/migrations/001_schema.sql`, `/.dev.vars`, `/agent.md`, `/functions/lib/auth.js`, `/tests/**`, `/HANDOFF-2026-08-02.md` → all 404 @ 1,512 B |
| Secret hygiene | Verified | `CLOUDFLARE_API_TOKEN` UNSET (presence-only check); OAuth acct `344944ed…` (account:read, user:read); `.dev.vars` gitignored+untracked |
| Production D1 | Empty | `appointments=0, gallery=0, contact_messages=0`; `whatsapp="", telegram=""` since 2026-07-30 |
| Custom domain | **Not attached** | Apex: 0 answers @ 1.1.1.1; www NXDOMAIN |
| "1500+" claim | Live, unsourced | `public/index.html:91` |
| Gallery orphan | Confirmed open | R2 `put` (`gallery/index.js:71`) precedes D1 insert; no compensating delete |
| Import bug | Confirmed open | `main.js:1039` fallback `'assets/clinic.jpg'` → API 400 → swallowed by `catch (e) {}` |
| Booking UX | Verified honest | Pre-hydration guard (`index.html:472,520`), double-submit guard (`main.js:648-652`), real failure state (`:722-735`), WhatsApp CTA gated on non-empty number (`:707,712`, FAB `:163`) |
| SEO | Open | Only `<meta description>`; no OG/canonical/JSON-LD/favicon/robots/sitemap |
| A11y | Partial | Fixed: `display=swap`, `--ink` sec-sub, hero scrim, autocomplete, `<main>`, 19/19 alt, arrow-key slider/grid. Open: no privacy notice, no modal Escape/focus-trap, no full WCAG pass |
| Housekeeping | Open | 11 MB + 352 KB PNGs tracked at root; 9 handoffs + 3 prompt files at root |
| Leaked token | Owner-only, unverified | Id `b17d8b13…`; not checkable from this machine (no API-Tokens scope); value untouched |
| T11 | Open | Zero notification code in `functions/`; doctor must open CMS |
| DOM tests | Open | No Playwright; contract tests parse `main.js`, never execute DOM |

Handoff discrepancies caught during the audit: production is `5423d45e` not `b4d74a16`; "www in neither var" is true only of the *serving* deployment (repo HEAD already carries it); `docs/handoffs/HANDOFF-2026-08-02.md` claims "untracked by design" but is committed.

*Audit ran read-only: no deploys, no D1 writes (SELECTs only), no file edits, no secret values read or printed. The single production POST was a tokenless probe that 403'd before any write path.*
